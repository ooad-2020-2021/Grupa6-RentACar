@model IEnumerable<RentAndGo.Models.BankovniRacun>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DatumValidnostiKartice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImePrezime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DatumValidnostiKartice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImePrezime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BrojRacuna">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BrojRacuna">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BrojRacuna">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
